version: '3'

services: 
    ci:
        build: 
            context: .
            dockerfile: Dockerfile.ci
        volumes:
            - ./build:/srv/annotations/build
        env_file:
            - dev.env
        depends_on:
            - web
